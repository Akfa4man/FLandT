<Window x:Class="FLandT_laba1_ver4.UI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:FLandT_laba1_ver4.UI.ViewModels"
        xmlns:uiTree="clr-namespace:FLandT_laba1_ver4.UI"
        mc:Ignorable="d"
        Title="ЛР1 - Лексический анализатор (Вариант 20)"
        Width="1100" Height="720"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <vm:MainViewModel x:Key="VM"/>
        <SolidColorBrush x:Key="OkBrush"   Color="#E6F6E6"/>
        <SolidColorBrush x:Key="OkBorder"  Color="#3BAA3B"/>
        <SolidColorBrush x:Key="ErrBrush"  Color="#FFF0F0"/>
        <SolidColorBrush x:Key="ErrBorder" Color="#D64545"/>
    </Window.Resources>

    <Window.DataContext>
        <StaticResource ResourceKey="VM"/>
    </Window.DataContext>

    <DockPanel LastChildFill="True">

        <!-- Верхняя панель с правилами и кнопками -->
        <Border DockPanel.Dock="Top" Padding="12" BorderBrush="#DDD" BorderThickness="0,0,0,1" Background="#FAFAFA">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Правила -->
                <!--<StackPanel Orientation="Vertical" Margin="0,0,12,0">
                    <TextBlock FontWeight="Bold" Margin="0,0,0,6" Text="Правила разбора:"/>
                    <TextBlock Text="Первый тип: (011)*000(001)* над {0,1}" Margin="0,0,0,2"/>
                    <TextBlock Text="Второй тип: [a|b|c|d]+, при этом 2-й и 3-й символы = a и c" Margin="0,0,0,2"/>
                    <TextBlock Text="Комментарий: строка, начинающаяся с #" Margin="0,0,0,2"/>
                    <TextBlock Text="Одинаковые типы слов должны быть разделены пробелом (или переносом/комментарием)" />
                </StackPanel>-->

                <!-- Кнопки -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Проверить" Command="{Binding RunCommand}" Margin="0,0,8,0" Padding="16,8"/>
                    <Button Content="Очистить" Command="{Binding ClearCommand}" Margin="0,0,8,0" Padding="16,8"/>
                    <Button Content="Копировать отчет" Command="{Binding CopyReportCommand}" Padding="16,8"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Центральная область: ввода и таблица токенов -->
        <Grid Margin="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.0*"/>
                <ColumnDefinition Width="0.05*"/>
                <ColumnDefinition Width="1.0*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="220"/>
            </Grid.RowDefinitions>

            <!-- Входной текст -->
            <!-- было -->
            <!-- <StackPanel Grid.Column="0" Grid.Row="0"> ... </StackPanel> -->

            <!-- стало -->
            <Grid Grid.Column="0" Grid.Row="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Входной текст:" Margin="0,0,0,6"/>

                <Border Grid.Row="1" BorderBrush="#DDD" BorderThickness="1" CornerRadius="6">
                    <TextBox Text="{Binding InputText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
             AcceptsReturn="True"
             AcceptsTab="True"
             FontFamily="Consolas, Courier New, monospace"
             FontSize="14"
             Padding="10"
             BorderThickness="0"
             VerticalAlignment="Stretch"
             HorizontalAlignment="Stretch"
             ScrollViewer.VerticalScrollBarVisibility="Auto"
             ScrollViewer.HorizontalScrollBarVisibility="Auto"/>
                </Border>
            </Grid>


            <!-- Разделитель -->
            <Border Grid.Column="1" Grid.RowSpan="3" Width="1" Background="#EEE"/>

            <!-- Дерево синтаксического разбора -->
            <StackPanel Grid.Column="2" Grid.Row="0">
                <TextBlock Text="Дерево разбора:" Margin="0,0,0,6"/>
                <TreeView ItemsSource="{Binding AstItems}">
                    <TreeView.Resources>
                        <HierarchicalDataTemplate DataType="{x:Type uiTree:TreeItem}"
                                      ItemsSource="{Binding Children}">
                            <TextBlock Text="{Binding Text}"
                           FontFamily="Consolas, Courier New, monospace"
                           FontSize="13"/>
                        </HierarchicalDataTemplate>
                    </TreeView.Resources>
                </TreeView>
            </StackPanel>


            <!-- Статус (OK/Ошибка) и счетчики -->
            <Border Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="3" CornerRadius="8" Padding="12" BorderThickness="1">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="{StaticResource OkBrush}"/>
                        <Setter Property="BorderBrush" Value="{StaticResource OkBorder}"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsOk}" Value="False">
                                <Setter Property="Background" Value="{StaticResource ErrBrush}"/>
                                <Setter Property="BorderBrush" Value="{StaticResource ErrBorder}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!-- Сообщения -->
                    <!-- Сообщения -->
                    <StackPanel Orientation="Vertical" Margin="0,0,8,0">
                        <!-- Статус -->
                        <TextBlock FontSize="16" FontWeight="Bold">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <!-- По умолчанию показываем ОК -->
                                    <Setter Property="Text" Value="Статус: OK"/>
                                    <Style.Triggers>
                                        <!-- Если IsOk == False — меняем текст -->
                                        <DataTrigger Binding="{Binding IsOk}" Value="False">
                                            <Setter Property="Text" Value="Статус: Ошибка"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <!-- Текстовый отчет -->
                        <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="6" Margin="0,6,0,0">
                            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Padding="8">
                                <TextBlock Text="{Binding ResultText}"
                       FontFamily="Consolas, Courier New, monospace"
                       FontSize="13"
                       TextWrapping="Wrap"/>
                            </ScrollViewer>
                        </Border>
                    </StackPanel>

                    <!-- Чипы со счетчиками (без Run) -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
                        <Border Background="#EEF3FF" BorderBrush="#99B" BorderThickness="1" CornerRadius="10" Padding="8,4" Margin="0,0,8,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="(011)*000(001)*: " />
                                <TextBlock Text="{Binding FirstCount}" />
                            </StackPanel>
                        </Border>
                        <Border Background="#FFF7E6" BorderBrush="#C9A35B" BorderThickness="1" CornerRadius="10" Padding="8,4">
                            <StackPanel Orientation="Horizontal">
                                <!-- на всякий случай оставляем только ASCII -->
                                <TextBlock Text="[a,b,c,d]+ (2-3=ac): " />
                                <TextBlock Text="{Binding SecondCount}" />
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </DockPanel>
</Window>